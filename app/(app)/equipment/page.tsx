import {EquipmentView} from "@/components/equipment-view";import {PageHeader} from "@/components/ui";import {getEquipment} from "@/lib/data/queries";export const metadata={title:"Equipment"};export default async function Page(){const data=await getEquipment();return <div className="mx-auto max-w-6xl p-4 md:p-8"><PageHeader eyebrow="Assets & upkeep" title="Equipment" description="Live runtime, maintenance records, and service schedules."/><EquipmentView data={data}/></div>}
