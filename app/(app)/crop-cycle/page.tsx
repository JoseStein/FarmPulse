import {CropCycleView} from "@/components/crop-cycle-view";import {PageHeader} from "@/components/ui";import {getCropCyclePageData} from "@/lib/data/queries";export const metadata={title:"Crop cycle"};export default async function Page(){const data=await getCropCyclePageData();return <div className="mx-auto max-w-5xl p-4 md:p-8"><PageHeader eyebrow="Active production cycle" title={`${data.cycle.crop.name} · ${data.field.name}`} description="Database-backed dates, stage, population, and yield records."/><CropCycleView data={data}/></div>}
