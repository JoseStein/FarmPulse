import { PageHeader,Status } from "./ui";import {Plus,Search,ChevronRight} from "lucide-react";
export function ModuleOverview({eyebrow,title,description,metrics,items}:{eyebrow:string;title:string;description:string;metrics:Array<[string,string,string]>;items:Array<[string,string,string]>}){return <div className="mx-auto max-w-6xl p-4 md:p-8"><PageHeader eyebrow={eyebrow} title={title} description={description} action={<button className="btn-primary"><Plus size={17}/>Add record</button>}/><div className="mb-5 grid gap-3 sm:grid-cols-3">{metrics.map(([l,v,n])=><div className="card p-5" key={l}><p className="text-xs text-slate-500">{l}</p><p className="mt-2 text-2xl font-bold">{v}</p><p className="mt-1 text-xs text-slate-400">{n}</p></div>)}</div><section className="card overflow-hidden"><div className="flex flex-col gap-3 border-b p-4 sm:flex-row sm:items-center sm:justify-between"><h2 className="font-bold">Pilot records</h2><div className="relative"><Search className="absolute left-3 top-3 text-slate-400" size={16}/><input className="input pl-9" placeholder="Search…"/></div></div><div className="divide-y">{items.map(([name,detail,status])=><button className="flex w-full items-center gap-3 p-4 text-left hover:bg-farm-50" key={name}><div className="min-w-0 flex-1"><p className="font-semibold">{name}</p><p className="truncate text-sm text-slate-500">{detail}</p></div><Status tone={status.includes("Low")||status.includes("Due")?"amber":"green"}>{status}</Status><ChevronRight className="text-slate-400" size={18}/></button>)}</div></section></div>}
